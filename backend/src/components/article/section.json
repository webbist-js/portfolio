{
  "collectionName": "components_article_sections",
  "info": {
    "displayName": "Article section",
    "description": "Numbered heading + body block"
  },
  "options": {},
  "attributes": {
    "heading": {
      "type": "string",
      "required": true
    },
    "kicker": {
      "type": "string",
      "description": "Optional mono label above the heading (fix pages)"
    },
    "body": {
      "type": "richtext",
      "required": true
    }
  }
}
